pipeline {
    agent any

    stages {
      stage('stage2') {
            steps {
                echo 'Hello World 2'
                sh '''
                    echo "stage2 is running"
                '''
            }
        }
     }
 }
